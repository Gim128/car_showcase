import React from 'react'
import Image from 'next/image'

"use-client";

const CustomButton = () => {
  return (
    <button
        disabled={false}
        type={"button"}
        className={`custom-btn`}
        onClick={() => {}}
        // 21:36
    >
      
    </button>
  )
}

export default CustomButton
